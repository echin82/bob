# Task: Create pull request

Create the pull request using the reviewed title and description.

Use the current pushed branch as the source branch.

Target the default base branch unless the repository requires a different target.

After creating the PR, return:

1. PR title
2. PR URL
3. Source branch
4. Target branch
5. Any next steps for reviewers
